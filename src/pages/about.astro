---
import Layout from "../layouts/Layout.astro";
import HeroIntro from "../components/shared/PageComponents/HeroIntro/HeroIntro.astro";
import BoldIntro from "../components/About/BoldIntro/BoldIntro.astro";
import ImagesRow from "../components/About/ImagesRow/ImagesRow.astro";
import ContentLogo from "../components/About/ContentLogo/ContentLogo.astro";
import PageHero from "../components/About/PageHero/PageHero.astro";
import StepsProcess from "../components/shared/PageComponents/StepsProcess/StepsProcess.astro";
import Values from "../components/About/Values/Values.astro";
import Steps from "../components/shared/PageComponents/Steps/Steps.astro";

import {
  Page_Seo_Query,
  Hero_Intro_Query,
  Steps_Query,
  Steps_Process_Query,
} from "../data/page/queries";

import {
  Bold_Intro_Query,
  Images_Row_Query,
  Content_Logo_Query,
  Page_Hero_Query,
  Values_Query,
} from "../data/about/queries";

const response = await fetch(`${import.meta.env.WPGRAPHQL_URL}`, {
  method: "POST",
  headers: {
    "content-type": "application/json",
  },
  body: JSON.stringify({
    query: `
		query aboutPageQuery {
			page(id: "/about", idType: URI) {
				${Page_Seo_Query}
        aboutPage {
          ${Hero_Intro_Query}
          ${Bold_Intro_Query}
          ${Images_Row_Query}
          ${Content_Logo_Query}
          ${Page_Hero_Query}
          ${Steps_Process_Query}
          ${Values_Query}
          ${Steps_Query}
        }
			}
		}
	`,
  }),
});

const {
  data: {
    page: { seoMetaTags, aboutPage },
  },
} = await response.json();
---

<Layout
  metaImg={seoMetaTags.seoMetaInformation.metaImage.sourceUrl}
  metaTitle={seoMetaTags.seoMetaInformation.metaTitle}
  metaDescription={seoMetaTags.seoMetaInformation.metaDescription}
>
  <HeroIntro data={aboutPage.heroIntro} />
  <BoldIntro data={aboutPage.boldIntro} />
  <ImagesRow data={aboutPage.imagesRow} />
  <ContentLogo data={aboutPage.contentLogo} />
  <PageHero data={aboutPage.pageHero} />
  <StepsProcess data={aboutPage.stepsProcess} />
  <Values data={aboutPage.values} />
  <Steps data={aboutPage.steps} />
</Layout>
